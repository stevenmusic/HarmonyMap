# HarmonyMap 專案規則

## 技術棧
- 單檔 HTML,沒有任何相依套件(只有 Google Fonts,離線時會退回系統字型)
- Canvas 2D 畫鍵盤
- 音源與 ScrollScore 相同:Salamander 鋼琴真實取樣為主,Web Audio 合成音色為備援

## 絕不能做的事
- 不要把單檔拆成多檔案,除非我明確要求(跟 ScrollScore 同一套部署方式)
- 不要改動照抄自 ScrollScore 的那一段鍵盤繪製程式(`isBlackKey` / `KB_*` /
  `computeKeyboardHeight` / `rebuildKeyboardRange` / `drawKeyboard`)。要加東西就像
  `drawKeyLabels` 那樣「在上面再疊一層」,不要動原本的函式本體
- 頂欄品牌名的 CSS 與 ScrollScore 逐項相同(字型、clamp 字級、字距、各斷點),
  改之前先確認兩邊還是一致
- 圖示一律用 ScrollScore 那一套線稿 SVG(24 格線、`stroke-width:2`、圓端點、
  `stroke:currentColor`),不要用 ⚙ ☀ 這種字元——字元的粗細與造型會隨系統字型跑掉,
  跟旁邊的線稿圖示放在一起會明顯不同掛。目前照抄的有:淺色模式切換、鍵盤設定齒輪
- 音名不要改成查表法。必須維持「字母照級數推、升降記號照音高差算」的規則,
  否則 Cdim7 會變成 C D♯ F♯ A 這種錯誤拼寫
- 不要把音源換成純合成音色。取樣清單(`PIANO_SAMPLES`)與網址是逐一驗證過的,
  改動前先確認檔案真的存在,套用不存在的組合只會讓抓取落空、整組退回合成音色

## 響應式斷點(與 ScrollScore 同一組,不要自己多開)
```
max-width:900 / 600 / 380 / 340    max-height:420    min-width:900
pointer:coarse ×3(單獨、+max-height:420、+max-width:420)
prefers-reduced-motion:reduce
```
- 這一組是 ScrollScore / SightScore / LoudNorm / HarmonyMap **四個專案共用**的,
  任何一邊要動都要四邊一起看。各專案只用得到的子集,但值一定要落在這組上
- SightScore 原本用 560 / 680 / 1024、LoudNorm 原本用 480 / 768 / 1200 / 1400,
  已經全部收斂到上面這一組;四個專案裡不會再出現這組以外的數字
- 需要「隨尺寸連續變化」的東西一律用 clamp / vh 算,不要為它新增斷點
  (例如釘住的鍵盤高度上限是 `min(KB_MAX_H, innerHeight * 0.22)`)
- `min-width:900px` 的兩欄版面必須放在整份樣式表的**最後**。寬度剛好 900px 時
  `max-width:900px` 與 `min-width:900px` 會同時成立,靠順序決勝負(ScrollScore 也是這樣排)

## 已知踩過的坑
- 和弦的延伸音(9/11/13)半音數會超過 12,拿去比對和弦字典時要先 mod 12,
  但推算拼寫時**不能** mod,不然會算錯字母與八度
- 窄螢幕把鍵盤釘在頂欄下方(`position:sticky`)之後,分組標題的 sticky 會躲進鍵盤底下,
  窄螢幕要把標題改回 `position:static`
- CSS 同權重的規則要寫在後面才蓋得過去:`.type-scroll` 的手機版覆寫必須放在
  桌機版那條規則之後,寫在版面區塊裡會失效
- 頂欄高度會隨字級與換行改變,鍵盤的 sticky 偏移量用 JS 量出來寫進 `--header-h`,
  不要寫死數字
- 取樣有 30 個檔案,等按下播放才開始抓的話,第一次播放一定只聽得到合成音色。
  所以在「使用者第一次碰到頁面」時就 `ensureAudio()` 暖機,不要退回按下播放才載入
- 響度:訊號鏈是 masterGain → loudnessComp(-18dB / 3:1)→ makeupGain(1.6)→ 限幅器。
  少了中間那段響度壓縮,平均音量會低到使用者得把裝置音量開到 80~90%。
  實測積分響度:合成備援 -9~-10 LUFS、取樣路徑最壞情況 -5 LUFS,峰值都在 0 dBFS 以下沒有削波
- 釘在鍵盤下方的那行摘要(`.kb-summary`)必須維持「絕對只有一行」(nowrap + 省略號)。
  它一旦會換行,底下整排和弦按鈕就會跟著上下跳,手指會點錯
- 同理,鍵盤區裡任何會出現/消失的東西(例如載入提示)都要用絕對定位,不能佔版面高度
- 上半部要維持「一個」sticky 區塊(`.topbar` 包住分頁 + 鍵盤 + 根音)。
  拆成三個各自釘住的元素會有兩個問題:元素之間的間距是透明的,底下內容會從縫隙穿出來;
  而且每個元素的 top 偏移量都要靠 JS 量前一個的高度
- `@media (max-height:420px)` 裡的 `.topbar{position:static}` 必須寫在 `.topbar` 基本樣式
  **之後**,同權重靠順序決勝負,寫在前面會被 `position:sticky` 蓋掉(手機橫向就會爆版)

## 字典
- 和弦 80 種、音階 120 種(14 組)
- 音階分組:大調七聲調式、和聲小調系統、旋律小調系統、和聲大調系統、雙和聲大調系統、
  五聲與藍調、日本、阿拉伯與中東、印度與其他亞洲、非洲、對稱、其他七聲、Bebop、進階與罕用。
  **母音階有七個調式的就要七個都列**——只放母音階、不放調式,等於承認這本字典不完整
  (和聲大調與雙和聲大調原本就只有母音階,散在「其他七聲音階」裡)
- **和弦按「家族」分組,不要按音數分組**。家族由三音 + 七音決定(大調系/屬和弦系/小調系/
  減系與半減系/無三度/增六),因為那才決定和弦的功能與可搭配的音階。按音數分組會把
  maj7→maj9→maj13 拆散,又把 maj7/7/m7/dim7 綁在一起(它們取五種完全不同的音階)
- 組內順序照張力音堆疊(三和弦 → 四音 → 五音以上),三和弦永遠排在家族最前面
- 風格、常用度、編制都是**篩選**不是分組。分組放錯會把和弦藏起來,篩選只是縮小範圍;
  而且風格是「誰常用它」(上下文),不是和弦的內在屬性
- 功能(主/屬/下屬)不能當分組軸——功能是上下文的屬性,同一個和弦在不同調裡功能不同。
  唯一的例外是增六和弦,它本來就是由解決方向定義的
- 音階維持按母音階系統與文化來源分組,那本來就是對的,不要跟著和弦一起改
- 跨文化同音異名(希賈茲 = 弗里吉安屬音階、羽調式 = 小調五聲…)是刻意保留的重複條目,
  **不要刪**——有人就是要查「拜拉夫」,刪掉等於這本字典對他失效。
  阿拉伯 10/14、印度 10/17 與西方音階同音高,處理方式是:
  晶片右上角標 `≡`(滑過去看得到等於誰)+ 一個「隱藏同音重複」開關(120→82)+
  結果面板互相連結。開關打開時搜尋仍然找得到被隱藏的條目,否則會幫倒忙
- 同音高不等於同一個東西:馬卡姆的拉斯特/巴亞提定義音是四分之一音(鋼琴只能近似),
  印度的塔特只是拉格的母音階(拉格還要看上行下行、vadi、演奏時辰)。說明欄要寫清楚
- 新增條目後跑一次「全部項目 × 21 個根音」的驗證(scratchpad/validate.js):
  拼寫、MIDI 自洽、音階內不得有重複音高、七聲音階七個字母各一次。
  兩種情形**不算錯**,寫驗證時要排除:級數本身就重複的音階(導音全音有 ♭7 與 7、
  佛朗明哥有 ♭3 與 3);以及需要三重升降時 `spell()` 刻意退回等音簡易拼寫(`acc` 為 null)
- 新增和弦時 `alias` 可以省略,讀的地方一定要 `def.alias && def.alias.length`——
  少了前半段,沒寫 alias 的和弦一點下去整頁就掛掉(踩過)

## 版面:一次只出一組(參考 Tenuto / musictheory.net)
- 清單的主要導覽是**分類按鈕**,不是一長條捲動清單。按一下換一整組,
  和弦一次最多 20 個、音階一次最多 20 個,而不是 80 / 120 個攤在同一頁
- 「全部」保留成第一顆按鈕(要一次看完整本字典的人還是有出口),選了它才會出現分組標題
- **組內照程度切成三層**:★必學 / 常用 / 進階。每一組把「它最基本的那一層」攤開,
  其餘收在「常用 N 種」「進階 N 種」展開鈕後面。屬和弦系 25 個裡有 17 個是變化音,
  全部平鋪出來的話,想找 C7 的人得先掃過 7♭5♯9。**沒有刪掉任何東西**,搜尋一樣找得到
- 三層與「程度」篩選必須是**同一套語意**。原本兩者各藏各的(收合藏 lv1、
  篩選的「常見以上」也藏 lv1),等於同一件事做兩次,而且互相看不出關係
- 「程度」篩選是**單選一層**,不是「以上」的門檻。門檻式永遠取不到「只有進階」;
  而且「常見以上」實際上做的是「排除進階」,使用者會讀成「全部」,意思剛好相反(踩過)
- 不收合的三種情形,不做的話會出現「這一組看起來是空的」:搜尋時;
  篩選已經指定某一層時;整組本來就只有一層時(增六和弦、進階與罕用、大調七聲調式)
- 選中的項目在收起來的那一層時(從順階和弦表、上下鍵跳過來)要自動展開那一層
- 音階也有 `lv`(必學 12 / 常用 48 / 進階 59),跟和弦同一套規則。
  母音階與實際會用的調式留在常用,冷門調式收進進階——**條目還在,只是不擋路**
- 分類是**導覽**、篩選是**縮小範圍**,兩者不能長得一樣:分類按鈕做成實心方鈕、
  字大一級,篩選維持細框膠囊,而且篩選預設收合
- 選中的項目不在目前分類裡(從順階和弦表、建議音階、上下鍵跳過來)時分類要自動跟過去,
  但**只能在選擇真的變了的時候校正**。每次 render 都無條件校正的話,
  使用者按別的分類會立刻被彈回去,等於按不動(踩過)
- 搜尋結果會跨分類,搜尋時要把分類列整排收起來,改用分組標題顯示
- 篩選把整組篩光時(例如「流行 × 三和弦」之下沒有增六和弦)要退回「全部」,
  否則按鈕不見了、清單也空著,看起來像壞掉
- `max-width:600px` 底下分類**不要排成一列橫向捲動**。音階有 15 顆(含「全部」),
  一列只看得到三顆,要左右滑才找得到想去的組,滑到一半還會忘記看過哪些(試過,不行)。
  改成「一顆按鈕顯示目前分類 + 點開後三欄格線」:收合時只佔一行,展開時五列一次掃完,
  不用捲也不用滑。三欄不能改兩欄——兩欄要排八列,最後一列會掉到摺線以下
- 展開後選了分類就要把面板收起來,面板蓋住的正是使用者接下來要看的清單
- 短螢幕(320×568)展開後面板仍會有一截在摺線下,展開時順手 `scrollBy` 把它捲進畫面,
  但只捲到釘住區塊的下緣為止,捲過頭會把鍵盤推走
- 窄螢幕判斷用 `matchMedia("(max-width:600px)")`,跟 CSS 讀同一個斷點,不要各寫各的
- 鍵盤的三個下拉(起始八度/琴鍵標示/播放方式)收在摘要列的 `⚙` 面板裡,不佔版面高度
- 不要照抄 Tenuto 的五線譜:這個工具的顯示區就是鋼琴鍵盤

## 介面上的記號
- 必學和弦:晶片**左上角一顆 ★**。不要改回「左緣金線」——一道色塊看不出是什麼意思
  (實際發生過:連做的人都要問「為什麼有些和弦左邊有金邊」)
- 同音重複:晶片**右上角 ≡**。兩個記號各佔一角才不會打架
- 兩個記號都要在篩選列下方寫出圖例。靠 `title` 屬性不算數,手機沒有 hover

## 播放方式
- 音階**不提供「齊奏」**:把一整個音階的音同時壓下去在音樂上沒有意義,
  那是和弦的呈現方式,音階是旋律性的,只給琶音與逐音上下行
- 每個分頁各自記住自己的播放方式(`PLAY_PREF`),在和弦選了齊奏、去音階看一眼
  (自動變成琶音)、再切回和弦時要回到齊奏,不要把音階的設定帶回來
- 音階的預設是**琶音**不是逐音上下行:逐音一次要跑十幾個音、長度是琶音的好幾倍,
  當「點一下聽聽看這個音階長怎樣」的預設太久;琶音快、一樣聽得出音階的顏色

## 反查(和弦辨識)
- 低音不是根音時要寫成**斜線和弦**:E G C 是 `C/E`,不是 `C`。和弦名稱說的是
  「這是哪一組音」,斜線後面說的是「最低的是哪一個音」,只寫 C 的話照著彈出來的
  聲音跟使用者選的不一樣。「這些音可以屬於」那一列也照同一套規則(`C6/E`)
- 斜線後面那個低音的拼寫**要照那個和弦自己的級數推**(`chordSpelling()`),
  不能用升降偏好硬拼——不然 C7 的第三轉位會寫成 `C7/A♯` 而不是 `C7/B♭`。
  德國增六的 ♯6 本來就拼成 A♯,所以它顯示 `CGr+6/A♯` 是對的,不是 bug
- 排序維持「低音當根音的排前面,再來是組成音少的寫法」。斜線和弦是同一組音的
  另一種讀法,不是次等結果,所以不另外降權
- 轉位編號看的是**低音是第幾級**:三音 = 第一轉位、五音 = 第二轉位、七音 = 第三轉位,
  升降不影響編號(♭3 一樣是第一轉位)。2/4/6/9/11/13 在低音**沒有**標準的轉位編號——
  那些是掛留音與延伸音,不是三度堆疊的其中一層,`C13/A` 講「第五轉位」是自己發明的
  說法。這種情形只寫「轉位」,斜線和弦名稱已經把低音是誰講清楚了
- 每一種讀法各自算自己的編號:B C E G 同時是 `Cmaj7/B`(第三轉位)與
  `Em♭6/B`(第二轉位),不能拿第一名的編號套用到整份清單

## 中英雙語
- 介面文字只有**一份 DOM**,靠 `data-i18n` / `data-i18n-attr` + `applyLanguage()` 換字。
  動態產生的內容(清單、篩選列、鍵盤摘要、結果面板)不吃 `data-i18n`,換語言後要 `render()`
- **英文介面不附中文對照名稱**。中文介面的副標放英文原名(查資料、看譜都用得到)是對的,
  反過來在英文版塞一個中文名稱讀不出任何資訊,只是雜訊——所以 `subName()` 在英文介面
  回傳空字串,呼叫端要連分隔號一起省略(用 `nameWithSub()`),不然會留下孤零零的「 · 」。
  空字串也不要硬塞進 DOM:`.chip` 與 `.kb-summary` 都是 flex + gap,空的 `<i>` / `.zh`
  會白白撐出一段間距,沒有內容時連元素都不要產生
- `sym` / `alias` / `t`(公式)這幾個欄位是**語言中性**的樂譜符號,不能放中文。
  放了的話兩種語言都會顯示同一串中文(踩過:`quartal` 的 `sym` 曾經是「四度堆疊」,
  英文版就長出 `C四度堆疊`)。中文名稱放 `zh`、英文名稱放 `en`,說明放 `d` / `d_en`
- 標點也要跟著語言換:別名的分隔號中文用「、」、英文用「, 」(`listSep`),
  英文句子裡不要出現全形空白 U+3000
- 新增條目時 `en` 與 `d_en` 是必填。少了就會退回中文,英文版立刻混進中文

## 驗證方式
改完樂理相關的程式後,至少確認這幾個拼寫是對的:
`Cdim7 = C E♭ G♭ B♭♭`、`F♯7♯9 = F♯ A♯ C♯ E G♯♯`、`D♭13 = D♭ F A♭ C♭ E♭ B♭`、
`C 大調順階七和弦 = Cmaj7 Dm7 Em7 Fmaj7 G7 Am7 Bm7♭5`
改完雙語相關的程式後,把語言切到英文,把兩個分頁的分類全部展開、每個條目都點一次,
確認畫面上除了語言切換鈕的「中」以外沒有任何中文字。
